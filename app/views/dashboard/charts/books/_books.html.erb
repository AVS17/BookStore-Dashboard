<section class="flex flex-col w-full m-2 space-y-2 border">
    <h3 class="text-lg font-bold text-white">Sells by Book </h3>
    <div class="flex flex-col " id="books_charts">
        <% Book.all.each do |book|%>
            <%= render 'dashboard/charts/books/chart', book: book %>
        <% end %>
    </div>
</section>  